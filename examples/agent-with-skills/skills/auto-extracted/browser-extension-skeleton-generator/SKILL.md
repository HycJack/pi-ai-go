---
name: browser-extension-skeleton-generator
description: Generate a functional browser extension project skeleton based on referenced codebases or feature requirements. Use when asked to "create a browser extension", "scaffold a Chrome/Firefox plugin", "build a plugin like project X", or "set up a Plasmo-based extension project". Covers project structure analysis, feature alignment, scaffold generation, and configuration.
---

# Browser Extension Skeleton Generator

> Auto-generated by autolearn from conversation

Generate a complete, buildable browser extension project skeleton based on existing reference projects or user requirements. Default to Plasmo for React/TypeScript development, with explicit fallbacks for vanilla JS or other frameworks. Prioritize rapid prototyping and cross-browser compatibility.

## Triggers

- "Write a browser extension that does..."
- "Scaffold a Chrome/Firefox plugin project"
- "Build a plugin similar to [existing project]"
- "Create a minimal extension skeleton using Plasmo"
- "I need a project structure for a browser add-on"

## Steps

1. **Analyze the reference project** — If the user provides a path to an existing project (e.g., `C:\Users\...\deepseek-pp`), attempt to read its structure and package manifest. If direct filesystem access is blocked, request permission or fall back to feature inference from the project name/description.
2. **Align on core features** — Propose 3–5 essential features for the extension based on the reference analysis or user description. Present them as a clear bullet list for user confirmation before generating code.
3. **Select the technology stack** — Default to Plasmo + React + TypeScript unless the user specifies otherwise. Justify the choice in a brief table (Framework, Frontend, API Layer).
4. **Generate the scaffold** — Create a unified project structure with the following files:
   - `popup.tsx` — Quick-access dialog when the extension icon is clicked
   - `sidepanel.tsx` — Persistent sidebar chat interface
   - `background.ts` — Service worker for API calls, context menus, and message routing
   - `content.ts` — Content script for text selection and page injection
   - `package.json` — Package dependencies and build scripts
   - `tsconfig.json` — TypeScript compiler options
   - `README.md` — Setup and development instructions
5. **Deliver configuration essentials** — Provide specific build commands (`pnpm dev`, `pnpm build`), manifest permissions required (`sidePanel`, `contextMenus`, `storage`), and instructions for loading the unpacked extension in Chrome.

## Output

A zipped or inline file listing containing a complete, ready-to-run browser extension project that can be loaded directly into Chrome's developer mode. All code should be syntactically complete and self-contained, requiring only an API key for the chosen AI service.

> Auto-generated by autolearn from conversation